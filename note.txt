# import de données dans une bdd via la méthode de QGIS QgsVectorLayerImport

#        proc = QProcess()
#        proc.start(ogrCommand)
#        proc.waitForFinished()        
        
#        # Create QGIS vector layer from thf
#        layerName = QFileInfo(filename).completeBaseName()
#        layer = QgsVectorLayer(filename, layerName, "ogr")
#        if not layer.isValid() or layer.type() != QgsMapLayer.VectorLayer:
#            return None
#        
#        # Set import parameters
#        table = 'thftemp'
#        geom = 'geom'
#        pk = 'id'
#        outUri = self.db.uri()
#        outUri.setDataSource( self.dialog.schema, table, geom, "", pk )
#        uri = outUri.uri()
#        providerName = self.db.dbplugin().providerName()
#        targetSrid = 2154
#        outCrs = QgsCoordinateReferenceSystem(targetSrid)
#        options = {}
#        overwrite = True
#        append = True
#        if overwrite:
#            options['overwrite'] = True
#        else:
#            options['append'] = True
#        singlePart = True
#        if singlePart:
#            options['forceSinglePartGeometryType'] = True
#        
#        # Do the import
#        ret, errMsg = QgsVectorLayerImport.importLayer( layer, uri, providerName, outCrs, False, False, options )
#        if ret != 0:
#            output = QgsMessageViewer()
#            output.setTitle( "Qadastre - import" )
#            output.setMessageAsPlainText( "Error %d\n%s" % (ret, errMsg) )
#            output.showMessage()
#            return
