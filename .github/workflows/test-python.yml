name: Tests

on:
  push:
    tags: 
      - '*'
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  test-qgis:
    name: Tests QGIS
    runs-on: ubuntu-latest
    
    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Running tests
        run: make docker-test
